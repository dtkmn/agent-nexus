# Example .env file for Agent Nexus
# Copy this file to .env and fill in your values

# Required: OpenAI API Key
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-key-here

# PostgreSQL Configuration (used by docker-compose)
POSTGRES_DB=quarkus
POSTGRES_USER=quarkus
POSTGRES_PASSWORD=quarkus

# Database Configuration for Quarkus (optional - defaults shown)
# For local development without Docker:
DB_USERNAME=quarkus
DB_PASSWORD=quarkus
DB_JDBC_URL=jdbc:postgresql://localhost:5432/quarkus

# For Docker Compose (automatically set in docker-compose.yml):
# DB_JDBC_URL=jdbc:postgresql://agent-nexus-postgres:5432/quarkus
